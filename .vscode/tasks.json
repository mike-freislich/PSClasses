{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++ -std=c++17 ${workspaceRoot}/src/main.cpp -o testSynth"
        },
        {
            "type": "shell",
            "command": "${workspaceRoot}/testSynth",
            "problemMatcher": [],
            "label": "run"
        }
    ]
}